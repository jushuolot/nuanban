# 已弃用 · Vue Admin

独立 **Element Plus** 管理后台已移除出部署路径。

## 请使用 PocketBase Admin

1. `docker compose up -d pocketbase`
2. 浏览器打开 **http://localhost:8090/_/**
3. 使用超管账号管理老人、订单、院校合作、导出任务等集合

若未来需要品牌化后台，可基于 PB REST 做薄壳；V1 不要求。
